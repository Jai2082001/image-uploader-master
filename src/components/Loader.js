import React from 'react'
import classes from './Loader.module.css'

const Loader = () => {
    return (
        <div className={ classes.loaderParentDiv }>
            <div className={ classes.loaderDiv}>
                
            </div>  
        </div>
    )
}

export default Loader